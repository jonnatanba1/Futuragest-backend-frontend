/**
 * Attendance domain entity type.
 *
 * This is a plain re-export of the Prisma-generated type — the Attendance model
 * is simple enough that it does not require a separate rich domain object.
 * Use-cases work directly with `Attendance` from @prisma/client.
 *
 * All fields are as defined in schema.prisma (see REQ-01 in the spec).
 */

export type { Attendance } from '@prisma/client';
