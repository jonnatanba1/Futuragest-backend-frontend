// Runtime config placeholder for `vite dev`. In production this path is served
// dynamically by nginx with the API_ORIGIN value (see nginx.conf), which shadows
// this static file. Left empty here so config.ts falls back to VITE_API_BASE_URL
// / the local default during local development.
window.__APP_CONFIG__ = {};
